-- 📌 [ระบบป้องกัน] รายชื่อเจ้าของสคริปต์ (เพิ่มได้หลายคนโดยใส่ไว้ในตาราง)
local ProtectedOwners = {
    "Onsenb47162",
    "Agakami_5625",
    -- อยากเพิ่มใครอีก ก็ใส่ "ชื่อในเกม" หรือ "DisplayName" ต่อท้ายตรงนี้ได้เลย คั่นด้วยคอมม่า (,)
}

local function isOwner(player)
    if not player then return false end
    -- วนลูปเช็คว่าชื่อของคนที่รัน ตรงกับใครสักคนในรายชื่อด้านบนไหม
    for _, ownerName in ipairs(ProtectedOwners) do
        if player.Name == ownerName or player.DisplayName == ownerName then
            return true
        end
    end
    return false
end

-- ==========================================
-- 📌 โครงสร้าง UI แบบใหม่ (มีแถบหมวดหมู่ด้านซ้าย + หน้าต่างกว้าง)
-- ==========================================
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local playerGui = localPlayer:WaitForChild("PlayerGui")

-- ลบ UI เก่าทิ้งก่อน (ถ้ามี)
local oldGui = playerGui:FindFirstChild("FlexibleMenuGui")
if oldGui then
    oldGui:Destroy()
end

-- สร้าง ScreenGui หลัก
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FlexibleMenuGui"
ScreenGui.Parent = playerGui
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 9999

-- 1. ปุ่มเปิด/ปิดหน้าต่างเมนู (สี่เหลี่ยมจัตุรัสเล็กๆ พร้อมรูปภาพ)
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
ToggleButton.BackgroundTransparency = 0.2
ToggleButton.Position = UDim2.new(0.05, 0, 0.1, 0)
ToggleButton.Size = UDim2.new(0, 45, 0, 45)
ToggleButton.Active = true
ToggleButton.Draggable = true
ToggleButton.Image = "rbxassetid://75925823414835"

-- บังคับโหลดภาพ (Force Preload)
task.spawn(function()
    pcall(function()
        game:GetService("ContentProvider"):PreloadAsync({ToggleButton})
    end)
end)

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 8)
ToggleCorner.Parent = ToggleButton

-- ==========================================
-- 2. หน้าต่างหลัก
-- ==========================================
local MainFrame = Instance.new("ImageLabel")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MainFrame.BackgroundTransparency = 1 
MainFrame.Position = UDim2.new(0.05, 0, 0.18, 0)
MainFrame.Size = UDim2.new(0, 550, 0, 450)
MainFrame.Visible = false
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Image = "rbxassetid://75925823414835"
MainFrame.ImageTransparency = 0.45

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

-- แถบหัวข้อด้านบน (Title Bar)
local TitleBar = Instance.new("TextLabel")
TitleBar.Name = "TitleBar"
TitleBar.Parent = MainFrame
TitleBar.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.Font = Enum.Font.GothamBold
TitleBar.Text = "  🤪 KTO CRUB รางตาย "
TitleBar.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleBar.TextSize = 15
TitleBar.TextXAlignment = Enum.TextXAlignment.Left

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = TitleBar

-- ปุ่มปิดหน้าต่าง (กากบาท X)
local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Parent = TitleBar
CloseButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseButton.Position = UDim2.new(1, -45, 0.5, -15)
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 14

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseButton

CloseButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- 3.1 แถบหมวดหมู่ฝั่งซ้าย (Sidebar)
local SidebarFrame = Instance.new("ScrollingFrame")
SidebarFrame.Name = "SidebarFrame"
SidebarFrame.Parent = MainFrame
SidebarFrame.BackgroundTransparency = 1
SidebarFrame.Position = UDim2.new(0, 10, 0, 50)
SidebarFrame.Size = UDim2.new(0, 140, 1, -60)
SidebarFrame.CanvasSize = UDim2.new(0, 0, 0, 300)
SidebarFrame.ScrollBarThickness = 4

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.Parent = SidebarFrame
SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
SidebarLayout.Padding = UDim.new(0, 8)

-- 3.2 พื้นที่แสดงหน้าต่างเนื้อหาฝั่งขวา (Container)
local ContentContainer = Instance.new("Folder")
ContentContainer.Name = "ContentContainer"
ContentContainer.Parent = MainFrame

-- รายชื่อหมวดหมู่ 6 หมวดฝั่งซ้าย
local categories = {
    "1. หน้าแรก",
    "2. ฟาม",
    "3. สังหาร",
    "4. esp",
    "5. ร้านค้า",
    "6. ตั้งค่า"
}

local pages = {}
local buttons = {}

-- ฟังก์ชันสำหรับสลับหน้าจอฝั่งขวา
local function switchPage(selectedName)
    for name, page in pairs(pages) do
        if name == selectedName then
            page.Visible = true
        else
            page.Visible = false
        end
    end
end

-- สร้างปุ่ม Sidebar และหน้า Page ฝั่งขวาอัตโนมัติ
for _, catName in ipairs(categories) do
    -- สร้างปุ่มหมวดหมู่ฝั่งซ้าย
    local btn = Instance.new("TextButton")
    btn.Name = catName .. "Btn"
    btn.Parent = SidebarFrame
    btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    btn.Size = UDim2.new(1, 0, 0, 35)
    btn.Font = Enum.Font.GothamSemibold
    btn.Text = catName
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 13
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    -- สร้างหน้าต่างเนื้อหาฝั่งขวา
    local page = Instance.new("ScrollingFrame")
    page.Name = catName .. "Page"
    page.Parent = ContentContainer
    page.BackgroundTransparency = 1
    page.Position = UDim2.new(0, 160, 0, 50)
    page.Size = UDim2.new(1, -170, 1, -60)
    page.CanvasSize = UDim2.new(0, 0, 0, 500)
    page.ScrollBarThickness = 4
    page.Visible = false
    
    local pageLayout = Instance.new("UIListLayout")
    pageLayout.Parent = page
    pageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    pageLayout.Padding = UDim.new(0, 10)
    
    pages[catName] = page
    buttons[catName] = btn
    
    -- เชื่อมปุ่มกดเพื่อสลับหน้า
    btn.MouseButton1Click:Connect(function()
        switchPage(catName)
    end)
end

-- ล็อกให้หน้า "1. หน้าแรก" แสดงขึ้นมาเป็นค่าเริ่มต้นหลัก
switchPage("1. หน้าแรก")

-- ==========================================
-- 📌 [ระบบเบื้องหลัง] ESP สีทอง + ป้ายชื่อ "ผู้สร้างสคริปต์" อัตโนมัติ
-- ==========================================
task.spawn(function()
    -- รายชื่อ Owner (ใส่ชื่อในเกม หรือ UserId ของคุณลงไปตรงนี้)
    local scriptOwners = {
        ["ชื่อในเกมของคุณ1"] = true,
        ["ชื่อในเกมของคุณ2"] = true,
        -- หรือใส่ UserID แบบตัวเลขก็ได้
        [123456789] = true,
    }

    -- ฟังก์ชันเช็คว่าเป็น Owner หรือไม่ (อิงจากระบบ isOwner ที่มีอยู่แล้วในสคริปต์คุณได้เลย)
    local function checkIfOwner(plr)
        if scriptOwners[plr.Name] or scriptOwners[plr.UserId] then
            return true
        end
        -- ถ้าในสคริปต์หลักของคุณมีฟังก์ชัน isOwner อยู่แล้ว สามารถเรียกใช้แทนได้เลย:
        if type(isOwner) == "function" and isOwner(plr) then
            return true
        end
        return false
    end

    -- ลูปทำงานเบื้องหลังตลอดเวลา
    while true do
        task.wait(1)
        pcall(function()
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr ~= localPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                    local char = plr.Character
                    
                    -- เช็คว่าเป็น Owner หรือไม่
                    if checkIfOwner(plr) then
                        
                        -- 1. สร้าง Highlight สีทองอร่ามรอบตัวละคร
                        local highlight = char:FindFirstChild("OwnerGoldHighlight")
                        if not highlight then
                            highlight = Instance.new("Highlight")
                            highlight.Name = "OwnerGoldHighlight"
                            highlight.Adornee = char
                            highlight.Parent = char
                            highlight.FillColor = Color3.fromRGB(255, 215, 0) -- สีทอง
                            highlight.OutlineColor = Color3.fromRGB(255, 255, 0) -- ขอบสีทองสว่าง
                            highlight.FillTransparency = 0.4
                            highlight.OutlineTransparency = 0
                        end
                        
                        -- 2. สร้างป้ายชื่อขึ้นบนหัว
                        local head = char:FindFirstChild("Head")
                        if head and not head:FindFirstChild("OwnerTagGui") then
                            local bill = Instance.new("BillboardGui")
                            bill.Name = "OwnerTagGui"
                            bill.Adornee = head
                            bill.Size = UDim2.new(0, 200, 0, 50)
                            bill.StudsOffset = Vector3.new(0, 2.5, 0)
                            bill.AlwaysOnTop = true
                            bill.Parent = head
                            
                            local textLabel = Instance.new("TextLabel")
                            textLabel.Size = UDim2.new(1, 0, 1, 0)
                            textLabel.BackgroundTransparency = 1
                            textLabel.Text = "👑 ผู้สร้างสคริปต์ 👑"
                            textLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
                            textLabel.TextScaled = true
                            textLabel.Font = Enum.Font.GothamBold
                            textLabel.TextStrokeTransparency = 0 -- ขอบดำให้ตัวหนังสือคมชัด
                            textLabel.Parent = bill
                        end
                        
                    end
                end
            end
        end)
    end
end)

-- รันเบื้องหลังแบบอัตโนมัติทันที
local UserInputService = game:GetService("UserInputService")
local player = Players.LocalPlayer

local function setupDoubleJump(char)
    local humanoid = char:WaitForChild("Humanoid")
    local rootPart = char:WaitForChild("HumanoidRootPart")
    
    local hasDoubleJumped = false
    local airPart = nil

    local function createAirPlatform()
        if airPart then airPart:Destroy() end
        
        airPart = Instance.new("Part")
        airPart.Size = Vector3.new(4, 0.5, 4)
        airPart.Position = rootPart.Position - Vector3.new(0, 3.5, 0)
        airPart.Anchored = true
        airPart.CanCollide = true
        airPart.Transparency = 1 -- ล่องหนมองไม่เห็น
        airPart.Parent = workspace
        
        task.delay(0.3, function()
            if airPart then
                airPart:Destroy()
                airPart = nil
            end
        end)
    end

    -- ดักจับจังหวะการกระโดดกลางอากาศเงียบๆ
    humanoid.StateChanged:Connect(function(oldState, newState)
        if newState == Enum.HumanoidStateType.Jumping then
            hasDoubleJumped = false
        elseif newState == Enum.HumanoidStateType.Freefall then
            local connection
            connection = UserInputService.JumpRequest:Connect(function()
                if not hasDoubleJumped and humanoid:GetState() == Enum.HumanoidStateType.Freefall then
                    hasDoubleJumped = true
                    createAirPlatform()
                    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    if connection then connection:Disconnect() end
                end
            end)
            
            local landedConn
            landedConn = humanoid.StateChanged:Connect(function(_, currState)
                if currState == Enum.HumanoidStateType.Running then
                    hasDoubleJumped = false
                    if connection then connection:Disconnect() end
                    if landedConn then landedConn:Disconnect() end
                end
            end)
        end
    end)
end

-- รองรับกรณีตัวละครตายแล้วเกิดใหม่ (รีเซ็ตระบบให้อัตโนมัติ)
if player.Character then
    setupDoubleJump(player.Character)
end
player.CharacterAdded:Connect(setupDoubleJump)
